import { Theme } from '@material-ui/core/styles';

export default (theme: Theme) => ({
  root: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    minHeight: "100vh"
  }
})
